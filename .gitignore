.env
__pycache__/
*.pyc
*.pyo
.venv/

# Large product dataset (359MB) — download separately
data/amazon_products_final.csv

# Qdrant / Postgres local volumes
.qdrant/
.postgres/

# macOS
.DS_Store

# Model binary weights — too large for GitHub (use HuggingFace instead)
models/*/adapter_model*.safetensors
models/*/optimizer.pt
models/*/rng_state.pth
models/*/scheduler.pt
models/*/training_args.bin

# Large tokenizer files (~11-16MB each) — not needed on GitHub
models/*/tokenizer.json

# Trainer state logs (44KB each, not needed)
models/*/trainer_state.json

# Poster binaries — generated files
eval/poster/*.pptx
eval/poster/*.pdf

# Eval scratch outputs
eval/smoke_test.md

# IDE / editor
.vscode/
.idea/
*.swp
*.swo
